#include "spawn_manager.h"

void check_and_spawn(app_t* app, wave_manager_t* wm)
{
	if (!wm)
		return;

	/*if (wm->current_enemy_num < wm->enemy_per_wave[wm->current_wave])
	{
		int required_enemies = wm->enemy_per_wave - wm->current_enemy_num;
		wm->current_enemies
	}*/
}